- clean: ['~']

- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.zshrc: zshrc
    ~/.zplug: zplug
    ~/.gitconfig: gitconfig
    ~/.gitconfig-launchbadge: gitconfig-launchbadge

- shell:
  - ["git submodule update --init --recursive", "Installing submodules"]